import dagre from '@dagrejs/dagre';
import type { Edge, Node } from '@xyflow/svelte';
import type { MaterialGraphEdge, MaterialGraphNode } from './types';
const NODE_WIDTH = 220; const NODE_HEIGHT = 86;
export const layoutWorkflow = (nodes: MaterialGraphNode[], edges: MaterialGraphEdge[]): { nodes: Node[]; edges: Edge[] } => { const graph = new dagre.graphlib.Graph().setDefaultEdgeLabel(() => ({})); graph.setGraph({ rankdir: 'TB', ranksep: 54, nodesep: 28, marginx: 24, marginy: 24 }); nodes.forEach((node) => graph.setNode(node.id, { width: NODE_WIDTH, height: NODE_HEIGHT })); edges.forEach((edge) => graph.setEdge(edge.source, edge.target)); dagre.layout(graph); return { nodes: nodes.map((node) => { const position = graph.node(node.id) ?? { x: 0, y: 0 }; return { id: node.id, type: 'materialGraph', position: { x: position.x - NODE_WIDTH / 2, y: position.y - NODE_HEIGHT / 2 }, data: node, draggable: false, selectable: true }; }), edges: edges.map((edge, index) => ({ id: edge.id ?? `${edge.source}-${edge.target}-${index}`, source: edge.source, target: edge.target, type: 'smoothstep', animated: edge.kind === 'fallback', class: edge.kind === 'fallback' ? 'material-graph-edge-fallback' : '' })) }; };
